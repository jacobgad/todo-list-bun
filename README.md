# todo-list-api

Install dependencies:

```bash
bun install
```

Setup database:

```zsh
bun run db:push
```

Run in dev mode:

```bash
bun run dev
```

Run in Production mode:

```bash
bun run start
```
