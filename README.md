# todo-list-api

Install dependencies:

```bash
npm install
```

Setup database:

```zsh
npm run db:push
```

Run in dev mode:

```bash
npm run dev
```

Run in Production mode:

```bash
npm run start
```
